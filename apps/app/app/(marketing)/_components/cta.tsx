/**
 * Final conversion band before the footer (repeat primary CTA).
 */
export function CTA() {
  return (
    <section className="px-6 py-20">
      <h2 className="font-semibold text-muted-foreground text-sm uppercase tracking-wide">
        CTA
      </h2>
      <p className="mt-2 max-w-2xl text-foreground leading-relaxed">
        Placeholder for a closing call-to-action—short headline, supporting
        line, and a prominent button. Captures visitors who scrolled the full
        page.
      </p>
    </section>
  );
}
