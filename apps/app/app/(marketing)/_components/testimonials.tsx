/**
 * Quotes or short stories from customers (optionally with photos and roles).
 */
export function Testimonials() {
  return (
    <section className="px-6 py-16">
      <h2 className="font-semibold text-muted-foreground text-sm uppercase tracking-wide">
        Testimonials
      </h2>
      <p className="mt-2 max-w-2xl text-foreground leading-relaxed">
        Placeholder for testimonials—real quotes, names, and roles (or case
        study links). Complements social proof with narrative depth.
      </p>
    </section>
  );
}
