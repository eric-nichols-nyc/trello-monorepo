# Auth (required for better-auth)
# Generate with: npx @better-auth/cli secret
BETTER_AUTH_SECRET=""

# App URL (for auth client and server - required for correct redirects)
NEXT_PUBLIC_SITE_URL="http://localhost:3021"
BETTER_AUTH_URL="http://localhost:3021"

# Database (uses shared DATABASE_URL from monorepo root .env.local)
# DATABASE_URL="postgresql://..."
