{
  "name": "security-lab-root",
  "private": true,
  "description": "Root workspace for the Security Lab client and server apps.",
  "scripts": {
    "dev:client": "pnpm dev --filter security-lab",
    "dev:server": "pnpm dev --filter server",
    "build:client": "pnpm build --filter security-lab",
    "start:client": "pnpm start --filter security-lab",
    "start:server": "pnpm start --filter server"
  }
}
